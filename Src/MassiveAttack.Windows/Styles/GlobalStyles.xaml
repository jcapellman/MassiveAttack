<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MassiveAttack.Windows.Styles">

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="pack://application:,,,/Fonts/#Gamefont" />
    </Style>
    
    <Style TargetType="{x:Type TextBlock}" x:Key="HoverOver">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Effect">
                    <Setter.Value>
                        <BlurEffect KernelType="Gaussian" Radius="2" RenderingBias="Quality" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Grid}" x:Key="MenuButton">
        <Setter Property="Background" Value="#042d5d" />
        <Setter Property="Height" Value="30" />

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#013e75" />
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="Header" BasedOn="{StaticResource HoverOver}">
        <Setter Property="Foreground" Value="Red" />
        <Setter Property="FontSize" Value="34" />
        <Setter Property="FontFamily" Value="{StaticResource MainFont}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Margin" Value="15,10,0,10" />
    </Style>

    <Style TargetType="Grid" x:Key="MainGrid">
       
    </Style>

    <Style TargetType="Button" x:Key="MainButton">
        <Setter Property="Background" Value="#042d5d" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="0,10,0,10" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontFamily" Value="{StaticResource MainFont}" />
        <Setter Property="Width" Value="150" />
        <Setter Property="Height" Value="30" />
        <Setter Property="BorderBrush" Value="#042d5d" />
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#013e75" />
                <Setter Property="BorderBrush" Value="#013e75" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>